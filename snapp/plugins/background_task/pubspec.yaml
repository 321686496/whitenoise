name: background_task
description: 声栖 OHOS 后台长时任务插件（音频后台播放 + 通知栏播放条）。
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      ohos:
        pluginClass: BackgroundTaskPlugin